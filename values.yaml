# 최상위 경로의 values.yaml

index-app:
  replicaCount: 
  image:
    tag: "1.0"
  service:
    port: 80
  containerPort: 80
  enabled: true

market-app:
  replicaCount: 
  image:
    tag: "1.0"
  service:
    port: 8000
  containerPort: 8000
  enabled: true

post-app:
  replicaCount: 
  image:
    tag: "1.0"
  service:
    port: 8000
  containerPort: 8000
  enabled: true